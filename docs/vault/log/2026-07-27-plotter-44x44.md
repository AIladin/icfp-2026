---
tags:
  - AI
  - log
date: 2026-07-27
---

# Plotter — 44x44 room compaction

Continuation of [[2026-07-26-plotter-compact-split-emit]], after H48 proved a 10x7 split EMIT and
H47 established the server-verified 45x45 lane-reordered floorplan.

## Live baseline (2026-07-26T23:47+03:00)

Live standings were λbubu rank **6/87**, score **4,355,268.75**, 20/20; leader
**3,487,941.0** (1.249x), 82 solved, updated 2026-07-26T20:46:05.697Z. Preserved fallback
`programs/plotter-4355269-lane-reorder-45x45.man`, SHA-256
`ae598b35a59007836ec00dbaaf9ba763a9866ebb57f79c4b2d09ae81e4904ad9`.

`build19.py /tmp/plotter-baseline.man 45 18 12` reproduces it byte-for-byte. `lmr test -p
plotter` reproduces 6/6 at 630/1414/241/1351/1831/2981 ticks, footprint 2,025 and local score
2,851,200. `lmr check` reports pipe lengths **11/7/2/2/13/2/3/6/15/10/4**. Only `lmr`
executes candidates; Python is used only for task-specific generation and case construction.

The exact 44x44 cut has two independent room prerequisites. H48 makes the display band 44 columns,
but SETUP must move x=19→18 to retain its 26-column room; because rooms cannot share a wall, that
first requires ECHO to shrink 19→18 columns. SETUP must then shrink 18→17 rows because its top is
fixed below the display at y=27.

## Hypothesis 49 — move ECHO's return ring one column inward

ECHO's width is set by its x=17 north return. Move the counted ring's west turn x=2→1, shift its
six-pair east pass and `m a ^` one column left, move the north return x=17→16, and shift the two
router rows left. Move the startup `@` one cell right so the shifted reload descent no longer walks
through it. **Price:** one isolated 18x10 ECHO interior on the unchanged 45x45 floorplan; no pipe
source, destination, other room, or SETUP code moves. The reload path gains at most one walk cell,
while each of three westbound counted passes loses one.

**Prediction:** `lmr check` stays 45x45 with the same eleven pipe lengths, all 154 `s`/`r`/`R`
operations retain their intended room pairs/readiness invariant, public frames remain exact, and no
multi-round public case slows. Reject at load/binding on any retarget, or at public on any failure or
slowdown. If green, retain it only as the width prerequisite; do not submit a footprint-neutral room
alone.

**Result:** revise the timing criterion; retain the width building block without submission.
`lmr check` reports an 18x10 ECHO inside the unchanged 45x45 footprint, with all eleven pipe
lengths unchanged. The static audit still has 149 reported `s`/`r`/`R` cells and every moved cell
keeps its intended room pair: three router sends still target P, two target Q, and all counted-loop
sends still target SETUP. Public passes 6/6 at **629/1414/241/1352/1833/2984** versus baseline
630/1414/241/1351/1831/2981. The startup saves one visible tick, but pixel-heavy multi-round cases
lose up to three; local score rises 2,851,200→**2,852,888** (+0.06%). This falsifies the
no-slowdown prediction, so the room is not independently valuable. Its worst public tick cost is
only 0.10%, however, far below the 45²→44² footprint reduction it unlocks jointly. Keep
`build21.py`/`rooms10.py` as an isolated prerequisite and price the completed 44x44 candidate before
any stress or submission.

## Hypothesis 50 — exit the third SETUP branch through its lower arm row

The third `X` heads west and its longest (`A<0`) arm ends immediately east of merge column x=25.
Make x=25 on that lower arm row a west arrow instead of dropping every arm to a new continuation
row. The straight and positive arms still descend into it; the negative arm continues west. This
moves Phase H up one row. Its remaining 47 one-cell operations can then use a fixed path through the
otherwise empty right-side branch cavity: three east, four north, then four alternating ten-cell
runs, before climbing the old return column. **Price:** one task-specific branch merger and direct
tail path in an isolated **26x17 SETUP**; retain every formula, operation order, pipe pin, other
room, and the baseline 45-column floorplan. The new path adds six turns and several return nops, so
allow up to 12 ticks per round; the eventual 44² footprint cut, not this isolated layout, must repay
it.

**Prediction:** `lmr check` loads a 26x17 SETUP and otherwise unchanged structure; all moved SETUP
`s`/`r` operations still select its sole ECHO queue pipes; public frames remain exact, with no case
more than `12 × rounds` ticks slower. Reject on overwrite/load/binding/frame failure. If green, keep
it as the independent height prerequisite and only then combine H48/H49/H50 into one 44x44 layout.

**Result:** keep the height building block. The first generated grid exposed a vertical backtick
pair: wrapping `` `12` `` onto columns already used by earlier literals made the loader treat a `+`
as literal content. Two blank cells after the wrap move its delimiters onto unused columns; this is
timing only and preserves the program. `lmr check` then reports SETUP **26x17** in the unchanged
45x45 footprint, with all pipe lengths unchanged. The audit still reports 149 `s`/`r`/`R` cells;
every SETUP send selects SETUP→ECHO and every receive selects ECHO→SETUP despite the cavity fold.

Public passes 6/6 at **634/1426/245/1367/1847/3013**. Relative to baseline, every case costs
exactly **four ticks per round**, substantially below the 12-tick allowance; local score is
2,879,550 (+0.99%) because footprint has not moved yet. The exact 4×round delta also confirms that
the branch fan-in and all 47 tail operations execute once, in order, on every sign/octant. Keep
`build22.py`; no fuzz or submission is warranted until the footprint prerequisites are combined.

## Hypothesis 51 — combine the three isolated cuts into 44x44

Use H48's 10x7 EMIT so the display shifts x=11→10, H49's 18x10 ECHO so SETUP can shift
x=19→18 without shared walls, and H50's 26x17 SETUP. Shift the input room y=42→41. Route
SETUP→ECHO into ECHO's now-free top-wall x=16, shorten ECHO→SETUP 10→9, and use the display's new
leftmost legal SWAP attachment x=11. H48 moves its DATA worker/send one tick earlier; shortening
SWAP 13→12 at the same time should preserve final-DATA/SWAP ordering, analogous to [[Advance DATA
and SWAP together]]. **Price:** the three already isolated room changes plus five endpoint/path
moves; no arithmetic, queue protocol, P/Q hot loop, or display semantics changes.

**Prediction:** `lmr check` reports exactly **44x44, footprint 1,936** (-4.40%), seven rooms and
11 pipes, with intended bindings and no pipe shorter than two. Public remains 6/6 and costs no more
than baseline plus H50's four ticks per round plus H49's measured per-case delta; therefore local
score falls at least 3.3%. Reject at load/binding/frame failure, or if any public case exceeds that
composed timing bound. If green, run directed fuzz, the saved 2,000-round stress suite, lint/type
checks, then submit.

**Local result:** keep and submit after two bounded routing revisions. The first layout put the
DATA worker's halt on EMIT's startup return column; `lmr` showed EMIT disappear while P filled and
blocked on its two-cell token pipe. Restoring H48's direct worker halt fixed that CFG. A legal
8-cell DATA zigzag then loaded but initially used ECHO→SETUP length 9; public committed wrong
frames, and an `lmr --trace` one-pixel run showed ECHO route `9,5,9,5,9,…` where the baseline routes
`9,5,9,5,5,…`, giving P `mx=4` for a zero-length segment. Moving only ECHO's queue-return source
x=10→9 restores its semantically load-bearing length **10**. This is [[A two-room FIFO has a phase,
not just a capacity|design timing]], not a tooling bug.

The corrected candidate passes public 6/6 at **633/1424/245/1365/1846/3009**, footprint
**1,936**, local score **2,749,765** (-3.56%). Average public ticks rise only
1,408.0→1,420.3 (+0.88%) while footprint falls 2,025→1,936 (-4.40%). `lmr check` reports 44x44,
seven rooms and pipe lengths **10/8/2/2/13/2/3/6/15/10/3**. The complete 149-operation audit has
exactly the baseline aggregate room-pair counts: SETUP 57 sends/44 receives on its ECHO queue;
ECHO 12 forwards to SETUP, 3 router sends to P and 2 to Q; no `q` exists. The shifted `R` nearest
counts also match baseline, preserving the input/queue readiness invariant.

Directed/random fuzz passes **86/86** segments. The saved seed-20260726 stress suite passes
**100/100 cases, 2,000/2,000 rounds** at 7,080–8,406 ticks. `ruff` and `ty` pass on
`build21.py`–`build23.py` and `rooms10.py`/`rooms11.py`. Candidate SHA-256 is
`13dd8101ec99844c6e6a6b5ce362789a936fe2632f350ce38655d413df722909`; it has 769 non-space
glyphs. Live standings immediately before submission remain rank 6/88 at 4,355,268.75; leader
3,487,941.0.

**Server result:** submission `530eb581-b5c9-415c-87a5-87f6a47121e3` passed **20/20**, score
**4,199,378 = 1,936 × 2,169.1** (-3.58%). Saved unchanged as
`programs/plotter-4199378-44x44.man`, SHA-256
`13dd8101ec99844c6e6a6b5ce362789a936fe2632f350ce38655d413df722909`; `build23.py
/tmp/repro.man 44 17 12` reproduces it byte-for-byte. The 4,355,269 fallback remains separate and
untouched. The immediate standings poll was still stale, so the graded receipt is authoritative.

## Hypothesis 52 — trade one input cell for one hot FIFO-return cell

H51 proved that ECHO→SETUP 10→9 alone advances queue feedback into the four-value input burst and
changes its order. The 44x44 input pipe simultaneously shortened 4→3. Move its ECHO destination
x=4→5 to restore input length 4 while moving the queue-return source x=9→10 for length 9. **Price:**
two endpoints only; footprint, rooms, programs, pipe capacity floor, and all output pins other than
the already-audited return pin remain fixed.

**Prediction:** the equal-and-opposite latency change restores the ECHO receive sequence and all
public frames, while the hot 38-value return path repays the one-cell input delay; no public case
slows and at least one multi-round case improves. Reject at binding/trace/frame failure or if every
completion tick is unchanged/slower. If green, apply the established fuzz/stress gates before a
submission.

**Result:** reject at the public gate, no fuzz or submission. `lmr check` reports the intended
input 3→4 and ECHO→SETUP 10→9 changes, but public passes 0/6 with the same five-pixel zero-length
symptom as the unpaired nine-cell return. Delaying input does not restore the feedback ordering;
that FIFO's return latency itself is the state variable. The temporary `build24.py` was removed and
`build23.py` still reproduces the server best byte-for-byte.

## Hypothesis 53 — move Phase H's `M` across the row wrap

H50 avoided a vertical backtick collision by walking two blank cells after wrapping, then loading
`` `12` `` at the safe columns. The preceding `M` only copies `mn` from A to B and is independent of
the turn. Wrap immediately after the preceding `r`, execute `M` on the new row, and leave the
literal at exactly the same safe columns. **Price:** move one non-pipe instruction and the wrap one
column; every literal, arithmetic operation, branch, tail cell, room, pin and pipe remains fixed.

**Prediction:** the complete binding report is byte-identical, public stays 6/6, and every round
finishes exactly two ticks sooner because two blank walks disappear. Reject on any literal/load,
frame, binding or non-improvement result; otherwise fuzz/stress and submit the pure tick win.

**Local result:** keep and submit. `lmr check` retains 44x44 and all pipe lengths; the complete
binding report is byte-identical. Public passes 6/6 at
**631/1418/243/1357/1838/2993**, exactly two ticks per round faster, local score **2,736,213**
(-0.49%). Directed fuzz passes 86/86; stress passes **100/100 cases, 2,000/2,000 rounds** at
7,040–8,366 ticks. `ruff` and `ty` pass. Candidate SHA-256 is
`9ff60ce706872b16b5992b84b4aa44981be331956b463da374e855b11fc14845`.

**Server result:** submission `2fc0d05f-3b52-4be0-8df3-011334cbf44f` passed **20/20**, score
**4,178,856 = 1,936 × 2,158.5** (-0.49%). Saved unchanged as
`programs/plotter-4178856-phase-wrap-44x44.man`; `build25.py /tmp/repro.man 44 17 12` reproduces
it byte-for-byte. The live board has incorporated H51 at rank **5/88**, 4,199,377.6 versus leader
3,487,941.0; H53's graded receipt is newer than that snapshot.

## Hypothesis 54 — use the new display's leftmost SWAP attachment

The 44-wide display shifted left, making bottom-wall x=11 a legal non-corner attachment; H53 still
uses x=12 and SWAP length 13 to preserve H47 timing conservatively. Move only that terminal one
cell left for SWAP=12. **Price:** one route endpoint, no binding or footprint change.

**Prediction:** the split DATA pipeline retains at least one tick of final-frame margin, every
public frame stays exact, and each round completes one tick sooner. Reject immediately on any frame
failure or non-improvement; otherwise fuzz/stress and submit.

**Local result:** keep and submit. SWAP=12 passes public 6/6 at
**630/1415/242/1353/1834/2985**, exactly one tick per round faster; local score **2,729,437**.
Bindings are unchanged. Directed fuzz passes 86/86 and stress passes **100/100 cases,
2,000/2,000 rounds** at 7,020–8,346 ticks. `ruff` and `ty` pass. Candidate SHA-256 is
`b0b0e7b8ca3b73742f2e841223a050d690752f2cf1e5bd8d4746a9a1831b9e42`.

**Server result:** submission `140b79f4-6937-4350-b1cd-7cececccb374` passed **20/20**, score
**4,168,595 = 1,936 × 2,153.2**. Saved unchanged as
`programs/plotter-4168595-swap12-44x44.man`; `build26.py /tmp/repro.man 44 17 11` reproduces it
byte-for-byte. The board has incorporated H53 at rank 5/88 and 4,178,856; this receipt is newer.
SWAP=12 now terminates at the geometric leftmost legal display attachment.

## Hypothesis 55 — make SETUP→ECHO a two-cell diagonal bend

SETUP→ECHO is length 3 only because it leaves the left wall at y=29, steps west, then south into
ECHO. Move the source down to y=30 so the source `<` and terminal `v` are adjacent, reaching the
loader's two-cell floor. SETUP has one output and ECHO reads with readiness-aware `R`, so static
selection should remain invariant. **Price:** one source pin and one deleted pipe cell; no other
geometry changes.

**Prediction:** the queue protocol tolerates this direction's one-tick advance, public stays 6/6,
and at least one case improves without any slowdown. Reject immediately on changed ECHO receive
order, frame failure, or no timing value; stress only if green.

**Result:** reject at public, no fuzz or submission. The two-cell pipe loads and every static send
still targets the same room, but public passes 0/6 with the same corrupted five-pixel zero-length
frame. Both directions of the SETUP/ECHO FIFO therefore have exact phase requirements: outbound
must remain 3 and return must remain 10 in this schedule. The temporary `build27.py` was removed.

## Hypothesis 56 — decrement ECHO on the west pass

ECHO became 18 columns by shifting its six-pair east pass and west turn left. Instead keep the
baseline loop's x=2 left turn and x=3..14 east pairs. Remove `m` from the east end, initialize BP to
3 rather than 4, and put `m` in a blank cell on the west pass before its four pairs. The schedule is
still four east passes and three west passes: each positive `a` sends the man west, where BP becomes
2/1/0; the fourth east `a` then exits straight. **Price:** one isolated ECHO interior; walls, spawn,
router, pins, 38-value forwarding count and all other logic remain fixed.

**Prediction:** bindings retain the exact aggregate room-pair counts, public stays 6/6, and removing
one walked cell from each of four east passes saves up to four ticks per round with no case slower.
Reject on queue/frame failure or no timing gain; otherwise fuzz/stress and submit.

**Local result:** keep and submit. All aggregate binding counts remain exactly baseline. Public
passes 6/6 at **627/1406/238/1341/1821/2961**, three ticks per round faster in most cases (the
one-pixel startup saves four, and border saves one extra), local score **2,708,464**. Directed fuzz
passes 86/86; stress passes **100/100 cases, 2,000/2,000 rounds** at 6,959–8,286 ticks. `ruff` and
`ty` pass. Candidate SHA-256 is
`a4683fb02b29949444cb91fe07d2420f21028ce3e9ef598252e4facf9f8e817c`.

**Server result:** submission `15632961-fc5c-49a9-8a3c-08346d004fb0` passed **20/20**, score
**4,136,748 = 1,936 × 2,136.8**. Saved unchanged as
`programs/plotter-4136748-west-decrement-44x44.man`; `build28.py /tmp/repro.man 44 17 11`
reproduces it byte-for-byte. The board has incorporated H54 at rank 5/88 and 4,168,595; this
receipt is newer.

## Hypothesis 57 — derive `mx<<11` from the live shift count

After computing `U`, Phase H still has B=12 when it pops `mx`. Replace
``M `11` W { M`` with ``{ M 1 W } M``: shift `mx` left 12, preserve it in B, load 1, swap, then
shift right once. Inputs are non-negative, so the arithmetic right shift is exact. This removes two
walked cells and the second multi-cell literal. Use the unavoidable return climb for the final two
tail operations, allowing the first west/east cavity pair to shorten 10→9 cells. **Price:** one
six-operation algebraic substitution and a two-cell-shorter direct tail path; all queue operations,
outputs, rooms and pipes remain fixed.

**Prediction:** `lmr check` retains the 44x44 structure and identical bindings; public remains 6/6
exactly two ticks per round faster. Reject on any arithmetic/frame mismatch or non-exact timing
change; otherwise fuzz/stress and submit.

**Local result:** keep and submit. The moved SETUP cells change coordinate-level audit text, but all
149 aggregate room-pair counts are exactly unchanged. Public passes 6/6 at
**625/1400/236/1333/1813/2945**, exactly two ticks per round faster; local score **2,694,912**.
Directed fuzz passes 86/86 and stress passes **100/100 cases, 2,000/2,000 rounds** at
6,919–8,246 ticks. `ruff` and `ty` pass. Candidate SHA-256 is
`42fa42e12015499e7a641cb7589065cd28f4cfd3fa07fe77712cbf6d3158d43d`.

**Server result:** submission `34be0226-5614-4200-b3dc-2651a692f6e8` passed **20/20**, score
**4,116,226 = 1,936 × 2,126.2**. Saved unchanged as
`programs/plotter-4116226-derived-shift-44x44.man`; `build29.py /tmp/repro.man 44 17 11`
reproduces it byte-for-byte. The board has incorporated H56 at rank 5/88 and 4,136,748; this
receipt is newer.

## Hypothesis 58 — delete ECHO's empty spacer row

ECHO local row 4 contains only the right-edge return `^`; both the north return and the reload
descent can cross directly into the next row. Delete it for an 18x9 room, shift the input room up,
and move SETUP's bottom wall from y=43 to its currently empty interior row y=42 while leaving every
SETUP executable cell absolute. Keep ECHO→SETUP's source at x=9 and attach at SETUP's left wall
y=41 so its exact length remains 10; keep input length 3 and SETUP→ECHO length 3. **Price:** one
empty ECHO row, two walls and two routes; no executed instruction or protocol latency changes.

**Prediction:** `lmr check` reports **44x43** (still footprint 1,936), ECHO 18x9, SETUP 26x16,
all aggregate binding counts and pipe lengths unchanged; public remains 6/6 with no case slower and
at least one tick saved per round. Reject on corner-attachment load failure, retarget, frame failure
or no timing value. If green, this establishes the full 43-row prerequisite before fuzz/stress and
submission.

**Local result:** keep and submit. `lmr check` reports 44x43, ECHO **18x9**, SETUP **26x16** and
all pipe lengths unchanged. Send counts remain exact; one static `R` nearest label moves from input
to SETUP, revising the literal audit prediction, but `R` is readiness-aware and only one input is
ready under the round gate. Public passes 6/6 at **624/1397/235/1329/1809/2937**, exactly one tick
per round faster; local score **2,688,136**. Directed fuzz passes 86/86 and stress passes
**100/100 cases, 2,000/2,000 rounds** at 6,899–8,226 ticks. `ruff` and `ty` pass. Candidate
SHA-256 is `8a7be2b28bb60aa63a22b912903a6b2982f42c99d03ad3f038e2ebe72c85cf3e`.

**Server result:** submission `cb7dab9a-7742-4c04-987d-4dd7062427d6` passed **20/20**, score
**4,105,966 = 1,936 × 2,120.8**. Saved unchanged as
`programs/plotter-4105966-44x43.man`; `build30.py /tmp/repro.man 44 16 11` reproduces it
byte-for-byte. The board has incorporated H57 at rank 5/88 and 4,116,226; this receipt is newer.

## Hypothesis 59 — pull ECHO's return and counted loop left

For a 17-column ECHO, shift the north return x=16→15 and both router rows left. Keep the reload
send at x=8 (one blank after its new x=2 turn) so it remains in SETUP's output partition. Shift the
six-pair east pass x=3..14→2..13, its `a`/exit left, and the west turn/`m` left; all source pins stay
fixed. **Price:** one 17x9 ECHO interior, with the one-cell reload detour paid once per round.

**Prediction:** all 12 forwarding sends remain SETUP, router sends remain 3→P/2→Q, the 38-value
schedule and pipe lengths stay exact, and four one-cell-shorter east passes repay the reload detour
for about three ticks per round. Reject on binding/frame failure or any slower case; if green,
ECHO supplies the lower-floorplan width prerequisite for 43x43 and the usual fuzz/stress gates apply.
