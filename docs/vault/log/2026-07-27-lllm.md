---
tags:
  - AI
  - log
date: 2026-07-27
---

Continues [[2026-07-26-lllm]], which is now the historical architecture/packing/shrink record. The
current baseline and new experiments live here so that the older log remains readable.

## 00:20 — live baseline, fallback, and specification refresh

Read the live released problem page, both prior LLLM logs, and the linked round/display/pipe/scoring/
packing notes, including [[lmp band compaction]]. The live specification remains: one well-formed
single-room LLLM program (`4 <= W,H <= 16`), one starting `@`, a 16x16 display, one initial frame,
then one frame after each `k in 1..64`, at most 30 rounds / 200 interpreted ticks, footprint-tick
scoring and a 15M LM tick cap.

Live standings at `2026-07-27T00:20:40+03:00`:

- team `lambda bubu` display name (`λbubu` in client configuration): **rank 9/57**, 52 solved;
- current server score **10,187,862,237**, 21/21;
- leader **1,178,367,085.7**, an **8.646x** gap.

Reproduced the current server-verified baseline without changing it:

```text
lmr check programs/little-little-little-man/baseline-safe-shrunk.man
# 189x163, footprint 35,721, 10 rooms, 17 pipes
lmr test programs/little-little-little-man/baseline-safe-shrunk.man \
  -p little-little-little-man
# 10/10, 178,139..603,259 ticks, local score 10,105,127,978
```

The older untouched fallback also remains reproduced and preserved:

```text
programs/lllm-16215808236-227x223.man
# 10/10, footprint 51,529, local score 16,072,528,907
```

The server-green 189x163 file is the baseline to beat; neither fallback will be overwritten.
Bounded `lmp` band compaction is not applicable to this already-routed, greedily shrunk `.man`.
It will be enabled and its report inspected if a new `.eman.toml` pack becomes relevant.

## 00:30 — Hypothesis 4: one more differential-safe deletion exists

**Priced hypothesis:** the 38 accepted columns changed enough geometry that a fresh one-cell deletion
pass over the 189x163 server baseline contains at least one further score-improving deletion which
is both public-green and frame-for-frame equal to the preserved server program on targeted stress.
First enumerate load-valid one-cell moves cheaply, then test promising moves sequentially. Falsifier:
no one-cell row or column survives public cases plus an expanded, baseline-derived `lmr` stress set.
The experiment is bounded to 352 candidates, one temporary grid at a time; no Python semantic oracle
and no unbounded search.

The existing 13-program stress run was rechecked: `baseline-safe-shrunk.man` still matches the
preserved fallback on every frame. It costs 3m38s only because the old script also reruns two obsolete
candidates and invokes `lmr` separately for every program, so the first implementation step is to
combine baseline-derived cases into one reusable stress JSON before trying deletions.

### 00:45 — first cut rejected; reusable stress gate built

`py/lllm_stress_cases.py` now generates
`programs/little-little-little-man/cases-stress.json` using **only the preserved server program run by
`lmr`** for expected frames. It combines the 13 cases which exposed the earlier private failure with
all 13 legal `(W,H)` counter/padding values, all three signs through `X`, `H`, and all four wall-hit
directions: **34 cases** total. The server-green baseline passes 34/34. One candidate invocation now
takes 19–35s instead of repeatedly running free mode to the step cap.

A cheap `lmr check` sweep found 95 load-valid one-column cuts. Descending public testing found the
first locally profitable one at current column 155: 188x163, public 10/10, local score
9,975,108,845 versus 10,105,127,978. It was immediately rejected by the new stress gate, **27/34**;
legacy fuzz 01/02/05/06/07/08/09 diverged in the same set that caught the old 20/21 submission.
Nothing was submitted. This confirms that public-only one-cell cuts can recreate the known hidden
opcode-path failure even after the prior safe replay.

### 01:10 — Hypothesis 4 survives on a different cut

A bounded follow-up (`py/lllm_find_safe_cut.py`) screened only columns below the rejected one: load
with `lmr check`, public cases at a reduced 1M screening cap, the known fuzz-02 canary, then all 34
stress cases. Columns 154/153/151/150 were public-red; current **column 149** passed every gate and
was preserved as `programs/little-little-little-man/baseline-safe-cut1.man`:

- 188x163, footprint 35,344;
- public 10/10, local score **9,975,108,845** (1.287% below the current local baseline);
- stress 34/34, including all seven old private-failure reproducers.

`py/lllm_audit_cut.py` parses topology from `lmr check` and applies the released nearest-segment rule
to every lowercase pipe operation. All **222/222** bindings are unchanged (`s=121, r=101, q=0`).
The minimum finite margin changes 1 -> 0: CPU `s` at old/new `(117,102)` becomes one reading-order
tie but still selects the same CPU->ROT pipe. Ties are specified behavior, but this makes server
validation especially important. Hypothesis 4 is confirmed locally; next gate is an ordinary-cap
public rerun and server submission, with the 189x163 file retained as fallback.

### 01:20 — server green, 10,056,568,013

Ordinary-cap `lmr test` reconfirmed 10/10 at 177,603–602,233 ticks; the combined stress stayed
34/34. Submission `74626178-f503-4062-b243-bc21d7746aad` passed **21/21**:

```text
score 10,056,568,013 = 35,344 (188x163) x 284,534.0 ticks
```

This is a 1.289% server improvement over 10,187,862,237. The standings endpoint was still serving
the old score immediately afterwards (`updatedAt 2026-07-26T21:46:06Z`), so rank movement is not
yet known. `baseline-safe-cut1.man` is now the server-verified baseline; retain the 189x163
`baseline-safe-shrunk.man` and original 227x223 file as fallbacks.

## 01:25 — Hypothesis 5: a second cumulative safe column exists

**Price/falsifier:** starting from the new server-green 188x163 grid, scan only current columns
148 downward (higher columns were already screened before cut 1). Each load-valid move gets all ten
public cases, then the known private-failure canary and all 34 differential cases; stop after one
accepted cut. Audit all 222 bindings afterwards. One sequential temporary grid, no search state; if
none survives, reject further one-column compaction and return to the netlist/room design.

### 01:35 — Hypothesis 5 confirmed locally

Current column **108** was the first survivor after 27 public-red load-valid cuts. Candidate
`baseline-safe-cut2.man` is 187x163 (footprint 34,969), public 10/10 at local score
**9,846,116,423**, and stress 34/34. Binding audit: all 222 unchanged, existing ties 1 -> 1, no new
tie. This is another 1.293% local reduction. It is preserved but not submitted yet; cut 1 remains
the server baseline.

## 01:40 — Hypothesis 6: a third cumulative safe column exists

Repeat the exact bounded gate from current column 107 down, stopping at one survivor. Falsifier is
no public+stress-green cut; price is at most 108 sequential one-cell probes and one binding audit.
Batching another cut before submission is worthwhile only if it remains fully differential-green.

### 01:45 — Hypothesis 6 rejected

Every load-valid column from 107 down was public-red; no candidate reached the differential gate.
The scan finished in 1m13s and wrote no cut3 file. This rejects a third cut in the previously
unscanned low-column range, not yet cuts above 107 whose feasibility may have changed after cut 2.
Before testing that weaker unlock hypothesis, server-validate the fully green two-cut batch.

Submission `ba9185d5-564e-437b-acdd-5a41c5014d41` then passed **21/21**:

```text
score 9,926,343,635 = 34,969 (187x163) x 283,861.2 ticks
```

This is 1.295% below cut 1 and 2.567% below the session's 189x163 baseline. Cut 2 is now the
server-verified baseline; cut 1 and the older files remain preserved fallbacks.

## 01:55 — Hypothesis 7: cut 2 unlocked a higher-column cut

The prior cut3 scan intentionally started at 107 because higher columns had been rejected against
cut 1. Deleting column 108 changed every higher pipe distance, so that evidence is not conclusive.
Run one full 186..108 rescan against cut 2, with the same public/canary/full-stress gates, stopping
at one survivor. Falsifier: no survivor; price at most 79 additional sequential probes.

### 02:00 — Hypothesis 7 rejected; one-cell compaction exhausted

All 47 load-valid cuts in current columns 186..108 were public-red. Combined with Hypothesis 6,
every column of the 187-wide baseline has now been reconsidered after cut 2; no cut3 exists. Per
[[Shrink tells you when to stop packing]], stop deleting global columns. Rows cannot lower the
187-cell max dimension and the current winning cuts already shorten every horizontal hot pipe by
two cells, so a new topology/room pack is the next lever.

## 02:05 — Hypothesis 8: extract the server-shrunk rooms and repack them

The winning raw shrink did more than move rooms: it compacted the room implementations themselves.
Its largest rooms are now CPU 167x40 including walls and EMIT 162x21, versus 202x48 / 202x32 in the
old reusable library. **Priced hypothesis:** extracting those exact interiors and their known routed
pins into new audited room types will let a one-variant `.eman.toml` pass logic-check and concrete
`--check`; if so, a bounded `lmp` search can lower max-dim below the current 187 toward the 167 CPU
floor. Falsifier: the recovered-layout hint cannot route with display windows around the known-green
52/114/180 lengths. Smallest experiment is extraction + logic-check + concrete check, no annealing.
